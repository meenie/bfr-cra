{
  "name": "bfr-cra",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "apollo-server-lambda": "^2.9.14",
    "graphql": "^14.5.8",
    "react-scripts": "^3.3.0"
  },
  "scripts": {
    "build": "cd client/ && yarn install && yarn run build && mv build ../"
  }
}
